# Template only. Do not commit real values. Create the real secret with:
#   kubectl -n hireai create secret generic hireai-secrets \
#     --from-literal=SECRET_KEY="$(python -c 'import secrets; print(secrets.token_hex(32))')" \
#     --from-literal=DATABASE_URL='postgresql://hireai:<password>@postgres:5432/hireai' \
#     --from-literal=POSTGRES_PASSWORD='<password>' \
#     --from-literal=CLOUDINARY_URL='cloudinary://<api_key>:<api_secret>@<cloud_name>' \
#     --from-literal=GROQ_API_KEY='<key>'
apiVersion: v1
kind: Secret
metadata:
  name: hireai-secrets
  namespace: hireai
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql://hireai:REPLACE_ME@postgres:5432/hireai"
  POSTGRES_PASSWORD: "REPLACE_ME"
  CLOUDINARY_URL: "cloudinary://REPLACE_ME"
  GROQ_API_KEY: ""
